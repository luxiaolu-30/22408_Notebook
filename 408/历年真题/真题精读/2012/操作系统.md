---
title: 2012 408 真题 · 操作系统
type: source-summary
domain: 408
subject: "408"
topic: 真题精读
year: 2012
created: 2026-09-14
updated: 2026-09-14
tags: ["408", 真题精读, "2012", 操作系统]
---

# 2012 408 真题 · 操作系统

> 本科目共 12 题 / 35 分：单项选择题 10 题（20 分）+ 综合应用题 2 题（15 分）。

## 一、单项选择题

### 23. （2 分 · 计算机系统概述）

下列选项中，不可能在用户态发生的事件是（ ）。

- **A.** 系统调用
- **B.** 外部中断
- **C.** 进程切换
- **D.** 缺页

**答案**：C

**解析**：答案 C。系统调用、外部中断、缺页都发生在用户态，由核心态完成处理；进程切换只能由操作系统在核心态执行，不可能在用户态发生。

> 难度 ⭐⭐ ｜ 章节 计算机系统概述 ｜ 标签 操作系统 / 计算机系统概述

---

### 24. （2 分 · 输入输出管理）

中断处理和子程序调用都需要压栈以保护现场，中断处理一定会保存而子程序调用不需要保存其内容的是（ ）。

- **A.** 程序计数器
- **B.** 程序状态字寄存器
- **C.** 通用数据寄存器
- **D.** 通用地址寄存器

**答案**：B

**解析**：答案 B。子程序调用只需保存断点（PC 内容）；中断处理除断点外还必须保存程序状态字（PSW），通用寄存器两者都需保存。

> 难度 ⭐⭐⭐ ｜ 章节 输入输出管理 ｜ 标签 操作系统 / I/O系统 / 中断 / DMA / 栈 / I/O方式

---

### 25. （2 分 · 内存管理）

下列关于虚拟存储器的叙述中，正确的是（ ）。

- **A.** 虚拟存储只能基于连续分配技术
- **B.** 虚拟存储只能基于非连续分配技术
- **C.** 虚拟存储容量只受外存容量的限制
- **D.** 虚拟存储容量只受内存容量的限制

**答案**：B

**解析**：答案 B。虚拟存储器允许程序部分装入、部分留在外存，只能建立在请求分页、请求分段等离散分配基础上；其容量由 CPU 寻址范围决定，不受内外存容量限制。

> 难度 ⭐⭐ ｜ 章节 内存管理 ｜ 标签 操作系统 / 内存管理 / 分页 / 分段 / 虚拟内存 / 页面置换

---

### 26. （2 分 · 输入输出管理）

用户程序发出磁盘 I/O 请求后，系统的正确处理流程是操作系统的 I/O 子系统通常由四个层次组成，每一层明确定义了与邻近层次的接口。其合理的层次组织排列顺序是（ ）。

- **A.** 用户级 I/O 软件、设备无关软件、设备驱动程序、中断处理程序
- **B.** 用户级 I/O 软件、设备无关软件、中断处理程序、设备驱动程序
- **C.** 用户级 I/O 软件、设备驱动程序、设备无关软件、中断处理程序
- **D.** 用户级 I/O 软件、中断处理程序、设备无关软件、设备驱动程序

**答案**：A

**解析**：答案 A。I/O 子系统自顶向下分为：用户级 I/O 软件、设备无关软件、设备驱动程序、中断处理程序。

> 难度 ⭐⭐ ｜ 章节 输入输出管理 ｜ 标签 操作系统 / I/O系统 / 中断 / DMA / 磁盘

---

### 27. （2 分 · 进程与线程）

假设 5 个进程 P0、P1、P2、P3、P4 共享三类资源 R1、R2、R3，这些资源总数分别为 18、6、22。T0 时刻的资源分配情况如下表所示，此时存在的一个安全序列是（ ）。

![T0 时刻的资源分配和最大需求表](q27-resource-table.webp)

- **A.** P0,P2,P4,P1,P3
- **B.** P1,P0,P3,P4,P2
- **C.** P2,P1,P0,P3,P4
- **D.** P3,P4,P2,P1,P0

**答案**：D

**解析**：答案 D。总分配量 (16,3,19)，Available=(2,3,3)，初始可满足 P1、P3、P4。先执行 P3，释放后 Available=(4,3,7)，可依次满足其余进程，安全序列为 P3、P4、P2、P1、P0。

> 难度 ⭐⭐⭐⭐ ｜ 章节 进程与线程 ｜ 标签 操作系统 / 进程管理 / 进程 / 线程 / 同步 / PV操作

---

### 28. （2 分 · 计算机系统概述）

若一个用户进程通过 read 系统调用读取一个磁盘文件中的数据，则下列关于此过程的叙述中，正确的是（ ）。

Ⅰ. 若该文件的数据不在内存中，则该进程进入睡眠等待状态

Ⅱ. 请求 read 系统调用会导致 CPU 从用户态切换到核心态

Ⅲ. read 系统调用的参数应包含文件的名称

- **A.** 仅Ⅰ、Ⅱ
- **B.** 仅Ⅰ、Ⅲ
- **C.** 仅Ⅱ、Ⅲ
- **D.** Ⅰ、Ⅱ和Ⅲ

**答案**：A

**解析**：答案 A。文件数据不在内存时产生缺页，进程进入阻塞（睡眠）等待调入，Ⅰ 正确；read 通过陷入使 CPU 从用户态切换到核心态，Ⅱ 正确；read 用 open 返回的文件描述符而非文件名作参数，Ⅲ 错误。

> 难度 ⭐⭐ ｜ 章节 计算机系统概述 ｜ 标签 操作系统 / 计算机系统概述 / 磁盘 / 文件系统

---

### 29. （2 分 · 进程与线程）

一个多道批处理系统中仅有 *P*1 和 *P*2 两个作业， *P*2 比 *P*1 晚 5ms 到达，它们的计算和 I/O 操作顺序如下：

*P*1 ：计算 60ms，I/O 80ms，计算 20ms

*P*2 ：计算 120ms，I/O 40ms，计算 40ms

若不考虑调度和切换时间，则完成两个作业需要的时间最少是（ ）。

- **A.** 240ms
- **B.** 260ms
- **C.** 340ms
- **D.** 360ms

**答案**：B

**解析**：答案 B。P1 先占 CPU：0～60ms 计算、60～140ms I/O；P2 自 60ms 计算，140ms 时让出，P1 先算 20ms 完成，P2 补算至 180ms，再 I/O 180～220ms、计算 220～260ms，共 260ms。

> 难度 ⭐⭐⭐⭐ ｜ 章节 进程与线程 ｜ 标签 操作系统 / 进程管理 / 进程 / 线程 / 同步 / PV操作 / 进程调度

---

### 30. （2 分 · 进程与线程）

若某单处理器多进程系统中有多个就绪态进程，则下列关于处理机调度的叙述中，错误的是（ ）。

- **A.** 在进程结束时能进行处理机调度
- **B.** 创建新进程后能进行处理机调度
- **C.** 在进程处于临界区时不能进行处理机调度
- **D.** 在系统调用完成并返回用户态时能进行处理机调度

**答案**：C

**解析**：答案 C。进程结束时、创建新进程后、系统调用完成返回用户态时都可进行调度；进程处于临界区只是访问临界资源，不妨碍处理机调度（如访问慢速外设时仍可切换），故 C 错误。

> 难度 ⭐⭐⭐ ｜ 章节 进程与线程 ｜ 标签 操作系统 / 进程管理 / 进程 / 线程 / 同步 / PV操作 / 进程调度

---

### 31. （2 分 · 进程与线程）

下列关于进程和线程的叙述中，正确的是（ ）。

- **A.** 不管系统是否支持线程，进程都是资源分配的基本单位
- **B.** 线程是资源分配的基本单位，进程是调度的基本单位
- **C.** 系统级线程和用户级线程的切换都需要内核的支持
- **D.** 同一进程中的各个线程拥有各自不同的地址空间

**答案**：A

**解析**：答案 A。无论是否支持线程，进程都是资源分配的基本单位，线程是调度的基本单位；用户级线程的切换无需内核支持，同一进程内线程共享地址空间，故 B、C、D 错误。

> 难度 ⭐⭐ ｜ 章节 进程与线程 ｜ 标签 操作系统 / 进程管理 / 进程 / 线程 / 同步 / PV操作

---

### 32. （2 分 · 输入输出管理）

下列选项中，不能改善磁盘设备 I/O 性能的是（ ）。

- **A.** 重排 I/O 请求次序
- **B.** 在一个磁盘上设置多个分区
- **C.** 预读和滞后写
- **D.** 优化文件物理块的分布

**答案**：B

**解析**：答案 B。重排 I/O 请求次序、预读与滞后写、优化文件物理块分布都能改善磁盘 I/O 性能；在磁盘上设置多个分区不能改善 I/O 性能，反而增加复杂性。

> 难度 ⭐⭐ ｜ 章节 输入输出管理 ｜ 标签 操作系统 / I/O系统 / 中断 / DMA / 磁盘

---

## 二、综合应用题

### 45. （7 分 · 内存管理）

某请求分页系统的局部页面置换策略如下：

- 系统从 0 时刻开始扫描，每隔 5 个时间单位扫描一轮驻留集（扫描时间忽略不计）
- 本轮没有被访问过的页框将被系统回收，放入空闲页框链尾，其中内容在下一次被分配之前不被清空
- 当发生缺页时，如果该页曾被使用过且还在空闲页框链表中，则重新放回进程的驻留集中；否则，从空闲页框链表头部取出一个页框

假设不考虑其他进程的影响和系统开销，初始时进程驻留集为空。系统空闲页框链表中页框号依次为 32、15、21、41。

进程 P 依次访问的 ⟨虚拟页号, 访问时刻⟩ 是：

⟨1, 1⟩、⟨3, 2⟩、⟨0, 4⟩、⟨0, 6⟩、⟨1, 11⟩、⟨0, 13⟩、⟨2, 14⟩

请回答下列问题：

(1) 访问 ⟨0, 4⟩ 时，对应的页框号是什么？

(2) 访问 ⟨1, 11⟩ 时，对应的页框号是什么？说明理由。

(3) 访问 ⟨2, 14⟩ 时，对应的页框号是什么？说明理由。

(4) 该策略是否适合于时间局部性好的程序？说明理由。

**答案 / 解析**：

答案：1、页框号 21。页 1、页 3 依次取走空闲链表头的 32、15，页 0 缺页时取第三个空闲页框 21。2、页框号 32。第 10 时刻扫描时页 1 未被访问被回收，其页框 32 仍留在空闲链表中；第 11 时刻页 1 再次访问，直接从空闲链表取回页框 32 放回驻留集。3、页框号 41。页 2 从未被访问，不在空闲链表中，缺页时取空闲链表头部的页框 41。4、适合。时间局部性好的程序，被回收的页很快会被再次访问，能直接从空闲链表取回，无需重新分配页框，策略优势明显。

> 难度 ⭐⭐⭐⭐ ｜ 章节 内存管理 ｜ 标签 操作系统 / 内存管理 / 分页 / 分段 / 虚拟内存 / 页面置换 / 链表 / 内存分配

---

### 46. （8 分 · 文件管理）

某文件系统空间的最大容量为 4 TB（$1\text{ TB} = 2^{40}\text{ B}$），以磁盘块为基本分配单位。磁盘块大小为 1 KB。文件控制块（FCB）包含一个 512 B 的索引表区。请回答下列问题：

(1) 假设索引表区仅采用直接索引结构，索引表区存放文件占用的磁盘块号。索引表项中块号最少占多少字节？可支持的单个文件最大长度是多少字节？

(2) 假设索引表区采用如下结构：第 0～7 字节采用 `<起始块号, 块数>` 格式表示文件创建时预分配的连续存储空间（其中起始块号占 6 B、块数占 2 B）；剩余 504 字节采用直接索引结构，一个索引项占 6 B。那么：

- 可支持的单个文件最大长度是多少字节？
- 为了使单个文件长度达到最大，请指出起始块号和块数分别所占字节数的合理值并说明理由。

**答案 / 解析**：

答案：1、磁盘块总数 = 4TB/1KB = 2³² 个，块号至少 32/8 = 4B；512B 索引区可放 512/4 = 128 个索引项，单文件最大 128×1KB = 128KB。2、连续区块数字段 2B 可表示 2¹⁶ 个磁盘块，即 2²⁶B；直接索引区 504B/6B = 84 项，即 84KB；故单文件最大长度为 2²⁶+84KB 字节。为使文件最大，起始块号和块数应各占 4B：起始块号 4B 可寻址 2³² 个磁盘块即整个 4TB 空间，块数 4B 也可表示最多 2³² 块（4TB），两者都达到系统容量上限。

> 难度 ⭐⭐⭐⭐ ｜ 章节 文件管理 ｜ 标签 操作系统 / 文件系统 / 文件 / 目录 / 磁盘

---

## See Also

- [[408/历年真题/真题精读/2012/数据结构]]
- [[408/历年真题/真题精读/2012/计算机组成原理]]
- [[408/历年真题/真题精读/2012/计算机网络]]
- [[408/历年真题/真题考点分析/2012]]
- [[408/历年真题/真题精读/真题精读总目录]]
