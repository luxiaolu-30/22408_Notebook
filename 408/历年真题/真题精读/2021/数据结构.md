---
title: 2021 408 真题 · 数据结构
type: source-summary
domain: 408
subject: "408"
topic: 真题精读
year: 2021
created: 2026-09-14
updated: 2026-09-14
tags: ["408", 真题精读, "2021", 数据结构]
---

# 2021 408 真题 · 数据结构

> 本科目共 13 题 / 45 分：单项选择题 11 题（22 分）+ 综合应用题 2 题（23 分）。

## 一、单项选择题

### 1. （2 分 · 线性表）

已知头指针 h 指向一个带头结点的非空单循环链表，结点结构为

```
------------
|data |next|
------------
```

其中 `next` 是指向直接后继结点的指针，`p` 是尾指针，`q` 是临时指针。现要删除该链表的第一个元素，正确的语句序列是（ ）。

- **A.** `h->next = h->next->next; q = h->next; free(q);`
- **B.** `q = h->next; h->next = h->next->next; free(q);`
- **C.** `q = h->next; h->next = q->next; if (p != q) p = h; free(q);`
- **D.** `q = h->next; h->next = q->next; if (p == q) p = h; free(q);`

**答案**：D

**解析**：答案 D。删除带头结点单循环链表的首结点：先用 q 抓住首结点，令 h->next = q->next 绕过它；若链中仅此一个数据结点（p == q），删除后须将尾指针 p 改为指向头结点 h，最后 free(q)。

> 难度 ⭐⭐⭐ ｜ 章节 线性表 ｜ 标签 数据结构 / 线性表 / 顺序表 / 链表 / 文件系统

---

### 2. （2 分 · 栈、队列与数组）

已知初始为空的队列 Q 的一端仅能进行入队操作，另外一端既能进行入队操作又能进行出队操作。若 Q 的入队序列是 1, 2, 3, 4, 5，则不能得到的出队序列是( )。

- **A.** 5,4,3,1,2
- **B.** 5,3,1,2,4
- **C.** 4,2,1,3,5
- **D.** 4,1,3,2,5

**答案**：D

**解析**：答案 D。该队列只有一端可出队、两端均可入队。先出 4、最后出 5，则 1、2、3 须在出队前排成 1,3,2，但三者按序从两端入队只能得到 321、213、312、123 之一，1 与 2 之间插不进 3，故 D 无法得到。

> 难度 ⭐⭐⭐⭐ ｜ 章节 栈、队列与数组 ｜ 标签 数据结构 / 栈 / 队列 / 数组

---

### 3. （2 分 · 栈、队列与数组）

已知二维数组 A 按行优先方式存储，每个元素占用 1 个存储单元。若元素 A[0][0] 的存储地址是 100, A[3][3] 的存储地址是 220 , 则元素 A[5][5] 的存储地址是( )。

- **A.** 295
- **B.** 300
- **C.** 301
- **D.** 306

**答案**：B

**解析**：答案 B。A[3][3] 比 A[0][0] 多 3 行 3 列，A[5][5] 多 5 行 5 列。由 220 = 100 + (3m+3) 得列数 m = 39，故 A[5][5] 地址 = 100 + 5×39 + 5 = 300。

> 难度 ⭐⭐ ｜ 章节 栈、队列与数组 ｜ 标签 数据结构 / 栈 / 队列 / 数组

---

### 4. （2 分 · 树与二叉树）

某森林 F 对应的二叉树为 T , 若 T 的先序遍历序列是 a, b, d, c, e, g, f , 中序遍历序列是 b, d, a, e, g, c, f , 则 F 中树的棵数是( )。

- **A.** 1
- **B.** 2
- **C.** 3
- **D.** 4

**答案**：C

**解析**：答案 C。由先序 a,b,d,c,e,g,f 和中序 b,d,a,e,g,c,f 可重建二叉树；森林转二叉树后，树的棵数等于根结点右链上的结点数。根 a 的右链为 a→c→f，共 3 个结点，故森林有 3 棵树。

> 难度 ⭐⭐⭐⭐ ｜ 章节 树与二叉树 ｜ 标签 数据结构 / 树 / 二叉树 / 二叉树遍历

---

### 5. （2 分 · 树与二叉树）

若某二叉树有 5 个叶结点，其权值分别为 10、12、16、21、30，则其最小的带权路径长度(WPL)是( )。

- **A.** 89
- **B.** 200
- **C.** 208
- **D.** 289

**答案**：B

**解析**：答案 B。哈夫曼树使 WPL 最小。贪心合并：10+12=22，16+21=37，22+30=52，37+52=89；WPL 等于所有非叶结点权值之和，即 22+37+52+89=200。

> 难度 ⭐⭐⭐ ｜ 章节 树与二叉树 ｜ 标签 数据结构 / 树 / 二叉树 / 二叉树遍历

---

### 6. （2 分 · 查找）

给定平衡二叉树如下图所示，插入关键字 23 后，根中的关键字是（ ）。

![2021 年第 6 题原图](408/历年真题/真题精读/2021/images/q06-source-1.webp)

- **A.** 16
- **B.** 20
- **C.** 23
- **D.** 25

**答案**：D

**解析**：答案 D。插入 23 后检查平衡因子，结点 20 失衡（BF=-2），其右孩子 30 的 BF=+1，属 RL 型双旋：先右旋 30，再左旋 20，25 上升为新根，故根的关键字为 25。

> 难度 ⭐⭐⭐⭐ ｜ 章节 查找 ｜ 标签 数据结构 / 查找 / 散列表 / B树 / AVL树

---

### 7. （2 分 · 图）

给定如下有向图，该图的拓扑有序序列的个数是( )。

![2021 年第 7 题原图](408/历年真题/真题精读/2021/images/q07-source-1.webp)

- **A.** 1
- **B.** 2
- **C.** 3
- **D.** 4

**答案**：A

**解析**：答案 A。拓扑排序每一步入度为 0 的结点都唯一，序列被唯一确定为 A→B→C→D→E→F，无任何分叉，故拓扑有序序列只有 1 个。

> 难度 ⭐⭐⭐ ｜ 章节 图 ｜ 标签 数据结构 / 图 / 最短路径 / 最小生成树

---

### 8. （2 分 · 图）

使用 Dijkstra 算法求下图中从顶点 1 到其余各顶点的最短路径，将当前找到的从顶点 1 到顶点 2, 3, 4, 5 的最短路径长保存在数组 dist 中，求出第二条最短路径后，dist 中的内容更新为（ ）。

![2021 年第 8 题原图](408/历年真题/真题精读/2021/images/q08-source-1.webp)

- **A.** 26, 3, 14, 6
- **B.** 25, 3, 14, 6
- **C.** 21, 3, 14, 6
- **D.** 15, 3, 14, 6

**答案**：C

**解析**：答案 C。dist 初值 [26,3,∞,6]。第 1 条最短路径到 3，松弛后 dist[2]=25；第 2 条到 5，松弛得 dist[2]=min(25,21)=21、dist[4]=14，即 [21,3,14,6]。

> 难度 ⭐⭐⭐⭐ ｜ 章节 图 ｜ 标签 数据结构 / 图 / 最短路径 / 最小生成树

---

### 9. （2 分 · 查找）

在一棵高度为 3 的 3 阶 B 树中，根为第 1 层，若第 2 层中有 4 个关键字，则该树的结点个数最多是( )。

- **A.** 11
- **B.** 10
- **C.** 9
- **D.** 8

**答案**：A

**解析**：答案 A。结点最多时根取 2 个关键字、3 个孩子；第 2 层共 4 个关键字按 2+1+1 分布，各结点孩子数等于关键字数加 1，第 3 层共 3+2+2=7 个结点，总计 1+3+7=11。

> 难度 ⭐⭐⭐⭐ ｜ 章节 查找 ｜ 标签 数据结构 / 查找 / 散列表 / B树

---

### 10. （2 分 · 排序）

设数组 S[] = {93, 946, 372, 9, 146, 151, 301, 485, 236, 327, 43, 892}, 采用最低位优先(LSD)基数排序将 S 排列成升序序列。第 1 趟分配、收集后，元素 372 之前、之后紧邻的元素分别是 (  )。

- **A.** 43, 892
- **B.** 236, 301
- **C.** 301, 892
- **D.** 485, 301

**答案**：C

**解析**：答案 C。LSD 第 1 趟按个位分配再收集：151,301,372,892,93,43,485,946,146,236,327,9。372 之前是上一非空桶末尾的 301，之后是同桶的 892。

> 难度 ⭐⭐⭐ ｜ 章节 排序 ｜ 标签 数据结构 / 排序 / 内部排序 / 外部排序 / 排序算法

---

### 11. （2 分 · 排序）

将关键字 6, 9, 1, 5, 8, 4, 7 依次插入到初始为空的大根堆 H 中，得到的 H 是（）。

- **A.** 9, 8, 7, 6, 5, 4, 1
- **B.** 9, 8, 7, 5, 6, 1, 4
- **C.** 9, 8, 7, 5, 6, 4, 1
- **D.** 9, 6, 7, 5, 8, 4, 1

**答案**：B

**解析**：答案 B。依次插入并上浮调整：6→[9,6]→[9,6,1]→[9,6,1,5]→[9,8,1,5,6]→[9,8,4,5,6,1]→[9,8,7,5,6,1,4]，大根堆层序序列为 9,8,7,5,6,1,4。

> 难度 ⭐⭐⭐ ｜ 章节 排序 ｜ 标签 数据结构 / 排序 / 内部排序 / 外部排序

---

## 二、综合应用题

### 41. （15 分 · 图）

已知无向连通图 G 由顶点集 V 和边集 E 组成，|E| > 0，当 G 中度为奇数的顶点个数为不大于 2 的偶数时，G 存在包含所有边且长度为 |E| 的路径（称为 EL 路径）。设图 G 采用邻接矩阵存储，类型定义如下：

```c
typedef struct {                    // 图的定义
    int numVertices, numEdges;      // 图中实际的顶点数和边数
    char VerticesList[MAXV];        // 顶点表，MAXV 为已定义常量
    int Edge[MAXV][MAXV];           // 邻接矩阵
} MGraph;
```

请设计算法 `int IsExistEL(MGraph G)`，判断 G 是否存在 EL 路径，若存在则返回 1，否则返回 0。要求：

(1) 给出算法的基本设计思想。

(2) 根据设计思想，采用 C 或 C++ 语言描述算法，关键之处给出注释。

(3) 说明你所设计算法的时间复杂度和空间复杂度。

**答案 / 解析**：

答案：1、基本思想：EL 路径即欧拉路径。无向连通图中，度为奇数的顶点个数为 0（存在欧拉回路）或 2（存在欧拉路径）时，存在包含所有边的路径。依次统计邻接矩阵每行非零元素个数作为各顶点度，记录奇数度顶点数 count，若为 0 或 2 返回 1，否则返回 0。
2、核心代码：

```c
int IsExistEL(MGraph G) {
    int count = 0;                        // 奇数度顶点个数
    for (int v = 0; v < G.numVertices; v++) {
        int degree = 0;                   // 第 v 行非零元素个数即 v 的度
        for (int i = 0; i < G.numVertices; i++)
            if (G.Edge[v][i] == 1) degree++;
        if (degree % 2 == 1) count++;
    }
    return (count == 0 || count == 2) ? 1 : 0;
}
```
3、时间复杂度 O(n²)（n 为顶点数，需遍历整个邻接矩阵），空间复杂度 O(1)。

> 难度 ⭐⭐⭐ ｜ 章节 图 ｜ 标签 数据结构 / 图 / 最短路径 / 最小生成树 / 图的存储

---

### 42. （8 分 · 排序）

已知某排序算法如下：

```c
void cmpCountSort(int a[], int b[], int n) {
    int i, j, *count;
    count = (int *)malloc(sizeof(int) * n);   // C++ 语言：count = new int[n];
    for (i = 0; i < n; i++) count[i] = 0;
    for (i = 0; i < n - 1; i++)
        for (j = i + 1; j < n; j++)
            if (a[i] < a[j]) count[j]++;
            else            count[i]++;
    for (i = 0; i < n; i++) b[count[i]] = a[i];
    free(count);   // C++ 语言：delete count;
}
```

请回答下列问题：

(1) 若有 `int a[] = {25, -10, 25, 10, 11, 19}, b[6];`，则调用 `cmpCountSort(a, b, 6)` 后数组 b 中的内容是什么？

(2) 若 a 中含有 n 个元素，则算法执行过程中，元素之间的比较次数是多少？

(3) 该算法是稳定的吗？若是，则阐述理由；否则，修改为稳定排序算法。

**答案 / 解析**：

答案：1、排序结果：b = {-10, 10, 11, 19, 25, 25}。
2、外层 i 取 0～n-2、内层 j 取 i+1～n-1，任意两个元素恰好比较一次，比较次数为 (n-1)+(n-2)+…+1 = n(n-1)/2。
3、不稳定。相等元素比较时走 else 分支使 count[i]++，靠前元素的计数变大而排到后面，破坏了相等元素的相对次序。将判断改为 if (a[i] <= a[j]) count[j]++; else count[i]++; 即可保持稳定。

> 难度 ⭐⭐⭐ ｜ 章节 排序 ｜ 标签 数据结构 / 排序 / 内部排序 / 外部排序

---

## See Also

- [[408/历年真题/真题精读/2021/计算机组成原理]]
- [[408/历年真题/真题精读/2021/操作系统]]
- [[408/历年真题/真题精读/2021/计算机网络]]
- [[408/历年真题/真题考点分析/2021]]
- [[408/历年真题/真题精读/真题精读总目录]]
