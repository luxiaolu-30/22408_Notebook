# 2026-08-13 工作日志

## 知识库健康检查 + 内容准确性核查
- 结构：孤立页 0、frontmatter 缺失 0、无冲突标记。
- **P0**：`计算机组成原理_第3版/` 目录被删除（13 文件，git 显示 D 未提交），导致 wiki/index 出现 11 个真实断链、统计「计组 11 篇」失真。待用户决定恢复或清理索引。
- **内容准确性**（逐条核对 + WebSearch 核实）：
  - ✅ 数学公式（三角/等价无穷小/泰勒）全部正确；408 数据结构（二叉树/排序/Dijkstra/散列表等）全部正确；政治分值结构、择校核心数据（中科院 120人/5万/2026线264考408、北航 88人/4.5万/调剂310）均正确。
  - **P1**：政治时政年份错位一年——2022 写「冬奥会」实为「恢复联合国席位50周年」（已核实），2023 写「一带一路十周年」属 2024。根因：时政按事件发生年而非考研年归类。
  - **P2**：择校对比文档中科院复试线仍写 260（2025），独立文件已更新 264（2026），未同步。
  - **P3**：毛中特30%旧口径、CSMA/CD 频率偏高、泰勒收敛域不严谨。
- 检查结果已写入 wiki/log.md（[2026-08-13] lint 条目）。待修复项：P0 计组目录（需用户确认）、P1 政治时政、P2 择校同步。

## 修复（用户确认后）
- 用户明确「不要恢复被删除的目录」→ 计组目录保持删除（撤销我此前的 git restore），清理 index.md 计组章节+统计表（总计 67→56）、408高频考点分析 See Also。
- 关键发现：P.md 被用户新建的 Process.md 替代（同 frontmatter + 新增「进展」章节），删除 P.md，index 中 [[P]]→[[Process]]。
- 内容修正：政治时政 5 处（2022 冬奥会→恢复联合国席位50周年、2023 一带一路十周年→百年大变局+全球发展倡议，均经 WebSearch 核实）；择校中科院复试线 260→264；泰勒收敛域、毛中特口径加注。
- 验证：lint 重跑，计组断链 0、孤立页 0。当前 md 文件 56 个。记录已写入 wiki/log.md（fix 条目）。
- 教训：Obsidian vault backup 插件会自动提交（15:10 已提交计组删除）；恢复/删除操作前应确认用户意图，勿自作主张恢复。

## Git 仓库损坏事故处理（严重）

### 现象
- 用户执行 `git pull --rebase` 时报 `could not mark as interactive: No such file or directory`
- 仓库状态错乱：`main` 变为 unborn（"No commits yet"），`origin/main` 指向缺失对象 `c3f30fa`，无任何 rebase 进行中

### 根因
- `.git/objects/pack/pack-08e0ba07...idx` 存在但其 `.pack` 数据文件丢失（verify-pack 报 "Cannot open existing pack file"）
- 该损坏包内含旧对象 `c3f30fa`（旧 origin/main）及 7 月若干 tree/blob；`.pack` 丢失后这些对象本地无法找回
- `c3f30fa` 是 `origin/main`(84d8993d) 的祖先，导致 origin/main 远古历史断档

### 修复步骤（已执行，数据零丢失）
1. 定位完好游离提交 `ffb696d`（实为本地+远程的合并提交，父为 57f67901 与 84d8993d）——即"解决冲突+拉取"已完成的状态
2. `git update-ref refs/heads/main ffb696d` 恢复 main
3. `origin/main` 的 update-ref 在 Git Bash/Windows 下静默失效（无法创建 refs/remotes/origin/ 子目录），改用手写 loose ref 文件 `.git/refs/remotes/origin/main` = 84d8993d 成功
4. 删除孤儿损坏包索引 `pack-08e0ba07.idx` 与可重建的 `multi-pack-index`

### 当前状态
- main = ffb696d（合并提交，本地+远程已合并，冲突已解决），领先 origin/main 2 个提交（本地未推送的 vault backup 提交）
- 工作树干净，用户文件（408/考前速看.md、.workbuddy/memory 等）完好
- `git log main -N` 正常；完整 `git log main` 会在 7 月旧历史处因 c3f30fa 缺失报错
- 未来 pull 新提交不受影响（用 84d8993d/5d0812f 等现存共同祖先）

### 待用户决策
- 是否执行 `git fetch --refetch`（会重新下载完整历史含 ~1.2GB 图片，修复 fsck 断档、使完整 git log 可用）
- 因 c3f30fa 是全部近期提交的祖先，无法只增量补回缺失的旧对象，只能整库重拉

## 推送结果（用户执行"推送当前提交"）
- `git push origin main` 返回 "Everything up-to-date"
- `git ls-remote` 确认远程实际 main = 本地 main = `ffb696d8439...` → 两次 vault backup 提交（含 408/考前速看.md 及今日工作）早已在远程，无数据丢失
- 顺手把本地 `origin/main` 缓存引用纠正为 ffb696d（之前因损坏包错位、解析失败），写 loose ref 文件成功
- 最终 `git status -sb` 显示 main...origin/main，ahead/behind = 0/0，仓库与远程完全同步

## 备注
- 全局 git 配置（pull.rebase=true, rebase.autoStash=true）此前已设，本次仍生效
- Git Bash 在 Windows 下 update-ref 写入 refs/remotes/<remote>/ 子目录时静默失败，需手写 loose ref 绕过
- 结论：之前担心的"未推送 2 个提交"实为 Obsidian Git 自动备份已推送，本地 origin/main 缓存才是错位的；远程历史完整，断档仅在本地 7 月旧对象（c3f30fa 等，远程完好）
